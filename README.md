# -minikube-nginx-deploy-
Deploy a custom HTML app using Docker, NGINX, and Kubernetes (Minikube). This project demonstrates how to containerize a static site, build a Docker image, and deploy it locally using Kubernetes YAML manifests.
